import java.util.scanner;

public class SimpleDotCom {
    public static void main(String[] args) {
    int[] locationCells;
    int numOfHits = 0;
    
    public String checkYouself (stringGuess) {
        int guess = Integer.parseInt(stringGuess);
        String result = "Мимо!";
        
        //if a 
    }
    
    void setLocationCells(int[] loc) {
    
    }
    
    }
}
